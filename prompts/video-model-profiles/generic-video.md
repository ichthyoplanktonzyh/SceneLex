# generic-video

- 面向当前较强的通用多模态视频模型。
- 支持纯文字生成一个连续短场景，也可能理解同一场景内的多个连续动作阶段。
- 优先使用 `direct_t2v` 和一个完整提示词；不要因为 storyboard 有多个 beat 就拆成多个 clip。
- 建议时长 8–15 秒，但由事件自然需要决定，不要求固定长度。
- 提示词可以描述事件先后、角色表演、简单镜头变化、环境连续性和可选声音。
- 只有角色/构图必须被精确锁定时才选择 `image_guided_i2v`。
- 只有单次生成明显超出一个连续事件的承载能力时才选择 `split_clips`。
