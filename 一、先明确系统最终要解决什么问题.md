# 一、先明确系统最终要解决什么问题

## 1. 开篇定位

SceneLex 要做的事情，可以概括成一句话：

> **构建一套把词义编译成可体验微世界的语义教学引擎，通过体验经验来获得对语义的感知，而不是通过 L1 的语言转述。**

这不是简单制作一批视频，而是要完成从语义的经验提取、微世界编译、交互体验呈现到跨语言映射的一整套系统。

## 2. 2026-08 理论校准：从“场景即释义”到“经验即词义”

本项目在早期曾提出“场景即释义”的核心主张，以及随之而来的“视频生成管线”。但随着研究深入，我们发现场景和视频只是经验的载体和呈现形式。

**“场景即释义”是早期相对朴素的表述，现在它已演进为更本质的：“经验即词义，微世界即体验”。**

视频管线（Shot Plan / Keyframe / Image / Video）作为早期架构，仍然被保留，但它被降级为“视频型微世界”的一种特定实现（Legacy）。目前的理论已经解除了对纯视频的系统性绑定，系统核心成为了“不限定媒介的语义引擎”。

## 3. 语言的本质：经验的符号系统

要想理解词义，必须先回答语言是什么。

语言不仅是交流的工具，更是人类对经验世界的符号化编码。词汇，正是人类对无数具身经验（embodied experience）进行范畴化（categorization）后所贴上的符号标签。

- **索绪尔（Saussure）与符号学**：语言是能指与所指的结合。
- **具身认知（Embodied Cognition, Lakoff and Johnson）**：人类的抽象概念最终都扎根于身体在物理世界中的感知与运动经验。
- **萨丕尔-沃尔夫假说（Sapir-Whorf）及相对论**：不同的语言系统以不同的方式切割和组织经验世界。

在这个视角下，学一个外语词汇，本质上不是去背它的拼写，也不是去背它的母语翻译，而是**去重新经历一遍那个词所对应的经验范畴**。

## 4. 核心问题：从符号翻译到经验直达

传统二语习得的最大痛点，在于它建立了一条“符号到符号”的翻译链条：

```text
L2 符号（比如 reluctant）
  ↓
L1 符号（比如“不情愿的”）
  ↓
概念/经验
```

这条路径导致学习者在听到 L2 时，大脑必须先调用 L1 才能触及经验，这不仅带来了严重的延迟（翻译脑），更致命的是，L1 和 L2 对经验的切割往往是错位的（比如汉语的“看”和英语的 look/see/watch/stare），从而产生大量的僵化错误和“中式外语”。

我们需要的是如同母语者习得语言那样的**直达路径**：

```text
L2 符号
  ↓
经验体验
  ↓
概念内化
```

系统的核心挑战，就是如何人工或自动地创造出能够精准承载特定词义的“经验”，让学习者去体验它。

## 5. 经验即词义：三层解耦

为了打破系统对视频或特定媒介的依赖，我们将核心概念进行了三层解耦：

**第一层：词义 ≈ 经验的范畴化（抽象层）**
词汇的本质是“经验的范畴化 + 语言共同体的约定”。它是高度抽象的，是无数相似经验的集合。

**第二层：场景 = 经验的具体实例（语义层）**
（实例化）
词汇太抽象无法直接体验，必须将其降维。一个“场景”就是该词义（经验范畴）中的一个具体实例。它包含了特定的行动者、环境、状态和关系。

**第三层：媒体 = 场景的载体（呈现层）**
（承载）
场景需要被呈现给学习者。这可以通过图片、一段短视频、一段 3D 动画、甚至是一个可交互的网页微组件来实现。

这三层解耦让系统获得了巨大的灵活性：只要我们能生成正确的“场景”（经验实例），就可以用任何最适合它的“媒体”去呈现。

## 6. 词汇的经验分类体系

不同的词汇编码了不同类型的经验。我们保留了原本的 10 大分类，但将其重新诠释为**经验类别**：

1. **实体型 (entity)**：对物理世界离散物体的感知（apple, ladder）。
2. **动作型 (action)**：身体或物体在空间中的动态过程（grab, pour, crawl）。
3. **属性型 (attribute)**：对物体特征的感知与评价（fragile, sticky）。
4. **状态变化型 (state_change)**：对事物随时间发生性质改变的观察（melt, recover）。
5. **空间与关系型 (spatial_relation)**：物体在空间中的相对位置与拓扑关系（under, behind）。
6. **心理状态型 (mental_state)**：内在的情绪、信念与感受（reluctant, jealous）。
7. **意图与行为型 (intentional_behavior)**：受目标驱动的动作序列（refuse, hesitate）。
8. **事件逻辑型 (causal_logic)**：事件之间的因果、转折与条件关系（despite, unless）。
9. **时间结构型 (temporal_relation)**：事件在时间轴上的分布与时相（already, suddenly）。
10. **认知与话语型 (cognitive_change / discourse_function)**：信念的更新以及交际中的语用操作（realize, actually）。

每一种经验类别，都需要用不同类型的体验来传递。

这 10 类经验可以归纳为约 5 种底层引擎能力：

| 引擎类型 | 覆盖的经验类别 | 微世界形态 |
|---|---|---|
| 3D 空间引擎 | 实体型、空间关系型 | 可旋转/探索的 3D 场景 |
| 时间轴/动画引擎 | 动作型、状态变化型、时间结构型 | 可控速度的动态过程 |
| 物理交互引擎 | 属性型 | 可操作的物体属性探索 |
| 叙事/情境引擎 | 心理状态型、意图行为型、认知话语型 | 情境代入 + 选择分支 |
| 逻辑推演引擎 | 事件逻辑型 | 条件模拟器 |

系统不需要做 10 套引擎，约 5 种底层渲染能力就能覆盖全部经验类别。

## 7. 微世界：词义的可体验编译产物

西摩·帕普特（Seymour Papert）提出了“微世界”（Microworld）理论，主张为学习者创造一个具备特定规则的小世界，让他们在其中探索并自然内化知识。

SceneLex 将每个“场景”（经验实例）编译为一个“微世界”。根据经验类别的不同，微世界的形态也会不同：
- **动作型**：可能是一个可控制时间轴的 3D 角色动画。
- **属性型**：可能是一个可以拖拽并观察其物理反应的交互组件（比如“sticky”会粘住鼠标）。
- **心理状态型**：可能是一个包含人物面部表情变化和内在独白的短视频（Legacy 视频管线在这里非常适用）。
- **空间与关系型**：可能是一个可旋转视角的拓扑结构图。

微世界的核心要求是：**学习者在微世界中的交互体验，必须与目标词汇的底层经验结构完全同构。**

## 7.1 分阶段演进：从文本叙事到交互微世界

完整的交互微世界（例如 3D 场景、物理模拟、叙事引擎）是系统的终极目标，但技术门槛高、迭代周期长。系统采用分阶段演进策略：

```text
阶段 1（当前）：经验叙事（纯文本，LLM 直接生成）
    ↓
阶段 2：富媒体增强（叙事 + 配图 + 音频）
    ↓
阶段 3：交互微世界（3D / 模拟 / 叙事引擎）
```

阶段 1 的核心洞察是：**微世界的第一种渲染器就是语言本身**。用 LLM 生成的经验叙事来替代词典释义，已经比“reluctant = 不情愿的”好了几个数量级。而且这些经验叙事本身就是未来构建交互微世界的“剧本”。

## 7.2 学习单元结构

每个词义的学习单元由以下组件构成，每个组件是一个独立可消费的卡片：

| 组件 | 作用 | 数据来源 |
|---|---|---|
| **经验溯源** | “这个词从什么经验中长出来” | LLM 编译（输入：WordSense） |
| **原型场景** | 建立核心经验锚点 | LLM 编译（输入：SceneSpec prototype） |
| **对比场景** | 划清经验范畴边界 | LLM 编译（输入：SceneSpec contrast） |
| **反例场景** | 排除错误泛化 | LLM 编译（输入：SceneSpec counterexample） |
| **边界场景** | 体验范畴边缘 | LLM 编译（输入：SceneSpec boundary） |
| **迁移场景** | 验证跨情境理解 | LLM 编译（输入：SceneSpec transfer） |
| **L1 经验对比** | 疗母语对同一经验的不同切割 | LLM 编译（输入：l1_confusables） |
| **自测题** | 检验理解 | 提取自 SceneSpec learning_tasks |

每张卡片独立可消费——用户可以只看经验溯源 + 原型场景就离开，也可以深度探索全部场景。背单词软件可以按复习阶段逐步释放卡片。

这套结构已通过 reluctant-01 原型验证（见 `prototype/reluctant-demo.html`）。

## 8. LLM 作为语义编译器

如何把抽象的词义转化为具体的微世界？我们的核心决策是：**将大语言模型（LLM）作为语义编译器。**

1. **放弃形式化语义描述语言**：我们不试图用某种复杂的逻辑公式去描述词义。与人类丰富的具身经验相比，形式化语言极其苍白。
2. **依赖 LLM 的隐式知识**：LLM 在海量人类文本的训练中，已经隐式地编码了极其丰富的具身经验和语义关系。它“懂”什么是 reluctant 的处境。
3. **重点约束输出**：我们不需要结构化的“输入”（词义解释），而是需要 LLM 像编译器一样，将词义编译出结构化的“输出”（微世界构建指令）。

编译过程：
```text
词汇 + 词汇类别 + 学习者状态
         ↓
    LLM（语义编译器）
         ↓
   微世界构建指令 (JSON, UI Spec, etc.)
         ↓
    渲染/呈现引擎
```

## 9. 跨语言：同一经验的多符号编码

在多语言习得中，L1 和 L2 面临着经验范畴不重合的挑战。

跨语言的本质是：同一片经验的汪洋，被不同语言的网捞起了不同形状的鱼。中文的“看”捞起了一大块，而英语用 look/see/watch 捞起了三小块。

所以跨语言对比不再是词典上的翻译对比，而是**经验范畴边界的碰撞测试**。我们要在微世界中设定特定的参数，让学习者体验到处于 look 和 see 边界上的那种微妙的情境差异。

## 10. 保留并对齐的设计

在新的理论框架下，早期项目的大量成果依然作为“语义层基础设施”被保留：

- **词义层建设（WordSense）**：依然是系统的核心单元，词汇的义项拆分和建模继续有效。
- **场景证据（SceneSpec）**：依然有效，它们现在被视为“经验实例”的文本记录。
- **五大场景组设计**：原型 (prototype)、对比 (contrast)、反例 (counterexample)、边界 (boundary)、迁移 (transfer) 这 5 种场景类型，完美契合了“建立经验范畴边界”的需求，继续作为验证体验的核心手段。
- **通用骨架与个性化渲染**：词义的通用经验结构依然有效，根据学习者的年龄、职业进行个性化微世界渲染的逻辑也依然有效。
- **L1 混淆集与对比体系**：针对特定母语者的易混淆体验，继续予以保留。
- **学习单元设计**：从体验到声音、从对比到输出的学习路径依然适用。
- **审核质量体系**：针对微世界的经验是否还原了目标语义，原有的语言、场景、教学审核体系依然关键。
- **连续语言扩展**：从单个词汇体验，向短语、句子和复杂语境体验扩展的演进路径不变。

## 11. 系统架构：语义编译引擎

SceneLex 现在是一个“语义教学引擎”，其架构分为四层：

1. **经验模型层 (Experience Model Layer)**
   维护 WordSense（义项）、相邻概念关系（Confusables）、L1 映射图谱，以及 10 大经验类别的本体逻辑。
2. **编译层 (Compilation Layer)**
   由 LLM 驱动，读取经验模型与学习者状态，利用隐式语义知识，将概念推理为具体的“微世界构建指令”（SceneSpec 的泛化版）。
3. **呈现层 (Rendering Layer)**
   接收指令，调用对应的渲染管线。可能是 Legacy 视频管线（生成分镜、提示词、调用 T2V），也可能是前端组件库（渲染交互式 UI），或者是 3D 引擎。
4. **交互层 (Interaction Layer)**
   学习者进入微世界，进行体验和验证。在这个层面上，系统收集用户的反应时间、边界识别准确率，以反馈验证“词义资源”的有效性。

## 12. 当前阶段与近期路线

系统当前的重心已从“建立视频生成闭环”转移到了“打通多模态语义编译引擎”。

1. 固化 WordSense 和 SceneSpec 数据结构，确保它们不受任何特定媒介的污染。
2. 开发针对不同经验类别（10 类）的 Prompt 编译器策略。
3. 探索非视频型的微世界呈现形式（例如交互组件）。
4. 跑通几个典型词义（例如 reluctant, sticky, unless）在不同介质上的端到端编译与体验展示。

这套系统最终将不仅是一个学习工具，它也是一种人类经验的高维度数字百科全书。
