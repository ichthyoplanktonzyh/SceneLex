我们要做的事情，可以概括成一句话：

> 构建一套“场景即释义”的语言学习系统，通过专门设计且可个性化的场景，让学习者不经过母语翻译，直接建立 L2 声音与概念、动作、关系和经验之间的连接。

这不是简单制作一批视频，而是要完成从**语义建模、场景设计、个性化生成、学习流程到效果验证**的一整套系统。

## 2026-07 架构校准：资源是核心，产品是消费者

SceneLex 最核心的产物可以进一步压缩为：

> **可验证的词义边界 + 能证明学习迁移的场景证据。**

这些产物首先是独立的、可版本化的语言资源。它们未来可以作为词典内容、课程素材、
播放器数据、API 返回、研究数据或学习实验输入，被多个消费者复用；不能绑定为某一个
SceneLex 学习应用的内部数据结构。

学习产品仍然重要，但它有两个明确角色：一是消费资源完成教学，二是通过即时理解、
相邻词区分、新场景迁移和延迟保持等指标验证资源质量。学习者身份、历史记录、UI、
网络协议和个性化状态属于消费端，不进入词义资源的权威模型。

同样，内容生产会使用多个大模型 API，但正式语义和场景资源不得包含厂商名、模型名或
请求参数。生成层按 OpenAI-compatible、Responses、Messages 或本地命令等协议适配；
切换厂商只改变适配器配置，不改变资源契约。

------

# 一、先明确系统最终要解决什么问题

传统学习路径通常是：

```text
L2 文字
→ L1 翻译
→ 概念
```

或者：

```text
L2 声音
→ L2 文字
→ L1 翻译
→ 概念
```

我们想建立的是：

```text
L2 声音
→ 场景与经验
→ 概念
```

这里最核心的目标不是“让学习者知道这个词怎么翻译”，而是让他知道：

> 在什么样的经验、状态和关系中，人会自然使用这个表达。

因此，我们的系统需要同时满足几个原则：

- 场景是主要释义媒介，不是装饰；
- 声音是主要语言入口，文字只在必要阶段出现；
- 场景必须针对具体词义设计；
- 一个词义不能只依赖一个场景；
- 场景要贴近学习者经验，但最终必须能跨场景泛化；
- 系统既要建立词义，也要逐渐建立句法、搭配和真实听说能力。

------

# 二、建立“词义场景化”的理论框架

第一项工作不是生成视频，而是建立一套稳定的理论模型。

我们需要回答：

> 什么样的意义可以怎样被场景表达？

不能只按传统词性分类，而应按“意义如何被感知”分类。

初步可以分为：

1. **实体型**
   apple、ladder、ceiling
2. **动作型**
   grab、pour、crawl、whisper
3. **属性型**
   fragile、sticky、narrow、messy
4. **状态变化型**
   melt、recover、improve、disappear
5. **空间与关系型**
   under、between、toward、behind
6. **心理状态型**
   reluctant、jealous、relieved、suspicious
7. **意图与行为型**
   refuse、hesitate、attempt、pretend
8. **事件逻辑型**
   despite、instead、unless、therefore
9. **时间结构型**
   already、still、eventually、suddenly
10. **认知与话语型**
    realize、suppose、apparently、actually

每一类都要形成对应的“场景表达方法”。

例如：

```text
动作词
→ 通过时间过程、动作方式和结果表达

心理状态词
→ 通过目标、预期、信息和行为冲突表达

逻辑词
→ 通过多个事件之间的关系表达
```

这一部分最终要形成一本内部使用的：

> **语义—场景设计手册**

------

# 三、建立词义层，而不是只建立单词层

系统的基本单位不能只是单词，因为一个单词可能有多个义项。

例如 `run`：

- 人跑步；
- 机器运行；
- 经营公司；
- 程序运行；
- 河流穿过；
- 库存耗尽。

因此数据结构应该是：

```text
word
└── sense
    ├── semantic structure
    ├── usage conditions
    ├── neighboring concepts
    ├── scene set
    ├── pronunciation
    ├── collocations
    └── sentence patterns
```

也就是说，我们要先完成：

1. 高频词表选择；
2. 词义拆分；
3. 义项优先级排序；
4. 相邻概念识别；
5. 常见搭配和句型整理。

我们最终制作的不是“3000 个单词”，而可能是：

> 3000 个高频单词中的 4000—6000 个核心义项。

------

# 四、为每个词义建立“语义骨架”

每个词义都需要先被拆成一组可描述的语义条件。

例如 `reluctant`：

```text
人物面对动作 A
人物不希望执行 A
存在外部原因推动人物执行 A
人物最终可能执行 A
抵触通过迟缓、表情或行为表现出来
```

`almost`：

```text
目标事件接近发生
必要条件几乎满足
最终结果没有发生
```

`instead`：

```text
原计划或预期是 A
A 没有执行
B 被选择并执行
B 替代 A
```

`realize`：

```text
事实一直存在
人物此前没有意识到
某个线索出现
人物的认知状态发生变化
```

这个语义骨架必须与具体人物、地点和文化脚本解耦，并接受跨场景、跨文化检验。
词义边界本身属于语言共同体的约定，因此这里追求的是可检验和可迁移，不宣称绝对文化中立。

它是系统的“深层结构”，后面的学生场景、职场场景、家庭场景，都只是不同渲染。

------

# 五、为每个词义设计场景组

不能采用：

```text
一个词义 = 一个视频
```

更合理的结构是：

```text
一个词义
= 原型场景
+ 对比场景
+ 反例场景
+ 边界场景
+ 迁移场景
```

## 1. 原型场景

用于第一次形成概念。

要求：

- 典型；
- 清晰；
- 低歧义；
- 目标词不可替代性较高。

## 2. 对比场景

帮助学习者理解它与相邻词的差异。

例如：

```text
look / see / watch
messy / dirty / chaotic
reluctant / hesitant / unwilling / refuse
```

## 3. 反例场景

告诉学习者“这不属于该词”。

例如：

- 人只是暂时没做，不一定是 reluctant；
- 人明确拒绝，可能是 refuse；
- 人还没做决定，可能是 hesitate。

## 4. 边界场景

设计接近但不完全符合的案例，帮助建立精确边界。

## 5. 迁移场景

更换人物、环境和事件，验证学习者是否形成了抽象概念，而不是只记住某段视频。

------

# 六、制定“好场景”的评价标准

每个场景都需要经过明确评审，而不是凭创作者感觉。

可以建立以下指标。

## 语义完整性

场景是否包含理解该词所必需的条件？

例如 `relieved` 必须包含：

```text
先担忧
→ 得知坏结果没有发生
→ 身心放松
```

不能只展示一个人在微笑。

## 低歧义性

学习者是否可能把它理解成另一个相邻词？

## 目标必要性

目标词是否真正参与理解场景，而不是被随意贴上去？

## 可观察性

原本不可见的心理、意图和关系，是否通过行为、结果和表情被外化？

## 声画时序

目标声音出现的时间是否合适？

通常应当是：

```text
先建立注意和预期
→ 关键语义发生
→ 目标表达出现
```

## 可迁移性

换一个生活环境后，学习者是否仍能识别该词？

## 记忆性

场景是否具有明显冲突、反差、意外或情绪，使它能够形成长期记忆？

------

# 七、建立 L1 特定的语义对比体系

我们不是简单绕开 L1，而是要理解：

> L1 已经塑造了学习者对世界的分类方式。

不同语言对经验的切分并不相同。

例如中文的“看”可能覆盖：

- look
- see
- watch
- stare
- glance
- glimpse

因此，对中文母语者来说，不能只是分别教这些词，还需要专门设计一套“重新切分概念空间”的对比场景。

这一层需要完成：

1. 分析 L1 与 L2 的概念边界差异；
2. 找出 L1 学习者常见的合并与混淆；
3. 找出容易产生负迁移的词；
4. 为每种 L1 设计不同的对比顺序；
5. 为不同 L1 设计不同的错误纠正场景。

因此系统应该区分：

```text
通用语义骨架
≠
L1 特定教学路径
```

词义可以通用，但教学入口和对比结构需要适配母语背景。

------

# 八、建立学习者经验模型

除了 L1，还要了解个人生活经验。

学习者画像至少可以包含：

- 年龄；
- 学生或职场阶段；
- 职业；
- 兴趣；
- 常见生活环境；
- 家庭角色；
- 学习目的；
- 熟悉的文化脚本；
- 已掌握词汇；
- 容易混淆的词；
- 已经看过和掌握的场景。

例如 `overwhelmed`：

- 程序员：报错、会议、消息同时出现；
- 学生：考试、作业和截止日期集中；
- 家长：孩子哭闹、电话响、饭烧糊；
- 医生：候诊患者不断增加。

深层词义相同，但经验入口不同。

------

# 九、建立“通用骨架—个性化渲染”系统

场景生成过程应当是：

```text
目标词义
+ 通用语义骨架
+ 学习者 L1
+ 学习者生活背景
+ 学习阶段
+ 需要区分的相邻词
→ 个性化场景组
```

个性化不只是换人物外貌和地点，而是要调整：

- 事件类型；
- 人际关系；
- 社会压力；
- 情绪表达；
- 冲突来源；
- 典型预期；
- 结果的重要程度。

例如 `embarrassed`，对不同人可以使用：

- 上课回答错误；
- 会议投屏弹出私人消息；
- 发错信息到工作群；
- 家庭聚会被公开追问私事。

------

# 十、防止过度个性化

个性化场景适合作为概念入口，但不能成为全部。

正确路径应该是：

```text
熟悉场景
→ 建立概念
→ 相似场景
→ 不同领域场景
→ 陌生场景
→ 抽象概念形成
```

也就是：

> 从我的世界进入，再扩展到共同世界。

否则学习者可能把 `reluctant` 错误地绑定成“加班时不情愿”，而没有理解它的一般意义。

------

# 十一、设计完整的学习单元

一个词义单元不能只是播放视频。

可以设计成以下流程。

## 阶段一：建立情境

先让学习者看见事件和关系，不显示文字。

## 阶段二：声音命名

在最合适的时间出现目标声音或短句。

## 阶段三：重复观察

从不同角度再次呈现，建立稳定声音—意义映射。

## 阶段四：对比辨别

让学习者在相邻场景中选择正确表达。

## 阶段五：跨场景识别

在新场景中判断目标词是否适用。

## 阶段六：声音识别

只听声音，选择对应场景。

## 阶段七：主动输出

给出场景，让学习者说出词、短语或句子。

## 阶段八：自然输入

把词放入更长的对话、故事或真实材料中。

完整路径是：

```text
概念建立
→ 概念区分
→ 跨场景泛化
→ 听觉识别
→ 主动调用
→ 自然语言整合
```

------

# 十二、从单词扩展到短语、句子和连续语言

仅仅建立词义还不能形成完整语言能力。

后续必须从：

```text
scene → word
```

扩展到：

```text
scene → phrase
scene → sentence
scene → dialogue
scene → narrative
```

例如学习 `spill`：

```text
spill
spill the water
He spilled the water.
He accidentally spilled coffee on his shirt.
Be careful not to spill it.
```

这样才能逐步获得：

- 语音切分；
- 搭配；
- 句法；
- 时态；
- 语气；
- 语用；
- 连续理解。

场景词典更像是整个系统的“接地层”，而不是最终全部课程。

------

# 十三、设计技术和内容生产管线

内容生产可以形成一个“语义场景编译器”。

语义资源与模型提示必须分层：`SceneSpec.storyboard` 表达教学证据的语义节拍，不能直接
当作未经翻译的视频 prompt。Director Agent 是核心中间层：理解词义证据与相邻概念边界，
再把场景翻译为适合当前视频能力的高质量提示词。完整说明见 `docs/production-workflow.md`。

## 输入

```text
词义
学习者 L1
学习者画像
词汇水平
目标对比词
```

## 中间处理

```text
义项识别与语义骨架提取
→ 必要条件、排除条件与可观察证据
→ 教学场景规格
→ Director 设计最小可见事件
→ 根据当前视频能力写高质量提示词
→ 直接 T2V，或按需使用参考图/I2V/少量拆片
→ Director 查看结果并修正语义偏差
→ 剪辑、声音与学习任务包装
```

## 输出

```text
模型无关的语义资源：原型、对比、反例、边界与迁移场景
Director Prompt：一个或少量模型适配的视频提示词、可选关键图提示词、简短 must-show/must-avoid
可替换的媒体产物：图片、短循环、动画、视频、音频或交互素材
可执行的学习任务与资源包
```

这套管线中，AI 可以承担：

- 语义初步分析；
- 场景创意生成；
- 分镜生成；
- 图像和视频生成；
- 多版本个性化；
- 配音；
- 练习生成。

但 AI 生成不能直接上线。模型审核可以提供语言、场景和教学质量参考；正式入库仍由人工
promote 决定，媒体还必须经过镜头级语义与连续性质检。学习实验可以用于未来优化资源，
但不再是“场景即释义”方法或规模化的前置门。

------

# 十四、建立审核和内容质量体系

AI 很容易生成“看起来合理，但并没有准确表达词义”的场景。

因此至少要有语言、场景和教学三层审核。审核可以由独立模型执行并由人工抽查，但
`schema-valid`、`reviewed` 与 `published` 必须保持不同含义；模型输出本身不拥有发布权。

## 语言审核

- 词义是否准确；
- 台词是否自然；
- 搭配是否真实；
- 语用是否合适。

## 场景审核

- 语义条件是否完整；
- 是否存在视觉干扰；
- 是否会指向错误概念；
- 场景是否符合目标文化经验。

## 教学审核

- 学习者能否通过场景推断；
- 是否过于明显或过于模糊；
- 是否真正训练了声音—意义映射；
- 是否能迁移到新场景。

------

# 十五、建立实验和评估方法

> 本节保留为未来效果评估参考。2026-07 决策已确认“场景即释义”为产品前提，
> 下述对照实验不再是内容发布或规模化的前置条件。

我们的核心假设必须被验证：

> 专门设计的个性化场景，是否比翻译、普通图片、随机影视片段和通用场景更能建立直接的声音—意义连接？

需要设计对照实验。

可以比较：

1. L2 单词 + L1 翻译；
2. L2 单词 + 普通图片；
3. L2 单词 + 随机影视片段；
4. L2 单词 + 通用设计场景；
5. L2 单词 + L1 适配场景；
6. L2 单词 + 个人经验适配场景。

测量指标包括：

- 首次理解正确率；
- 即时记忆；
- 一周后保持率；
- 只听声音时的反应速度；
- 新场景识别能力；
- 相邻词区分能力；
- 主动输出能力；
- 是否依赖中文回忆；
- 学习者主观顿悟感和负担感。

最重要的不是“记住了视频”，而是：

> 能否在没有原视频的情况下，理解和使用目标表达。

------

# 十六、选择合适的 MVP

第一版不应直接做几千词，也不应在制作契约稳定前批量生成视频。

最终 MVP 可以达到约 30—50 个词义，但媒体生产应先从 `messy`、`reluctant`、`almost`
这类不同语义类型中各选一个样本，走通“语义证据—Director Prompt—视频生成—查看修正”闭环，
记录真实失败模式后再决定需要哪些额外控制和自动化。

例如：

- 具象名词：5 个；
- 动作词：8 个；
- 属性词：8 个；
- 心理状态词：8 个；
- 时间和逻辑词：8 个；
- 易混淆词组：3—5 组。

优先选择：

- 场景能够清晰表达；
- 传统翻译难以形成听说直觉；
- 相邻词容易混淆；
- 学习效果容易测试；
- 有明显个性化空间。

例如：

```text
messy / dirty / chaotic
look / see / watch / glance / glimpse
reluctant / hesitant / unwilling / refuse
almost / eventually / suddenly
instead / despite
realize / notice / discover
```

------

# 十七、第一版产品可以先做成什么

MVP 不应默认每个词义都生成同样长度的视频。Director Agent 应根据语义证据和当前模型能力选择：

可以使用：

- 连续图片；
- 动态漫画；
- 简单动画；
- 实拍微短片；
- AI 视频；
- 交互式场景。

第一版重点是验证语义设计和生产工作流，而不是追求影视品质。需要因果与表演的原型短片
可优先尝试约 10—15 秒、3—4 个镜头；这只是制作启发，不是所有词义的固定格式。

产品界面可以非常简单：

```text
播放场景
→ 播放目标声音
→ 选择对应场景
→ 查看对比场景
→ 在新场景中判断
→ 尝试说出表达
```

------

# 十八、建立内容资产和数据结构

长期来看，需要建立几个核心数据库。

## 词义数据库

- 单词；
- 义项；
- 词频；
- 难度；
- 发音；
- 搭配；
- 句型。

## 语义骨架数据库

- 参与者；
- 动作；
- 状态；
- 目标；
- 预期；
- 结果；
- 情绪；
- 因果；
- 时间；
- 信息状态。

## 对比关系数据库

- 近义词；
- 反义词；
- 上下位词；
- 易混淆词；
- L1 特定混淆。

## 场景模板数据库

- 原型模板；
- 对比模板；
- 边界模板；
- 迁移模板。

## 学习者模型

- L1；
- 背景；
- 熟悉场景；
- 已掌握词义；
- 错误模式；
- 记忆表现。

------

# 十九、需要处理的产品边界问题

我们还需要持续回答几个问题。

## 是否完全禁用文字

不一定。

初始接地阶段可以尽量避免文字，但在后期学习拼写、阅读和句法时，文字仍然必要。

关键是顺序：

```text
先声音—意义
后声音—文字—意义整合
```

而不是从一开始就让文字抢走意义通道。

## 是否完全禁用 L1

也不必绝对化。

系统的核心是让 L1 不成为默认中介。但在处理极难抽象概念、纠正误解或提高效率时，L1 可以作为辅助验证。

## 是否所有词都能场景化

不是所有词都能仅靠一个短视频完成，但大量词可以通过：

- 多事件关系；
- 对比；
- 认知状态；
- 说话者预期；
- 交互任务；

进行接地。

对于极度抽象或元语言词汇，可能需要场景、简单 L2 解释和多实例共同完成。

------

# 二十、整个项目的实施顺序

按照依赖关系，可以分成六个阶段。

## 第一阶段：定义框架

完成：

- 项目目标；
- 词义分类；
- 语义骨架格式；
- 场景组结构；
- 质量评价标准。

## 第二阶段：制作语义资源纵向切片

完成：

- 6—10 个高价值目标词义；
- 近义词和对比关系；
- 中文母语者常见混淆；
- 每个词义的语义骨架；
- 可执行、可评分的学习任务和资源导出。

## 第三阶段：建立模型无关媒体生产工作流

先为少数不同语义类型制作：

- 原型；
- 对比；
- 边界；
- 迁移场景。

- 一个尽可能直接的高质量视频提示词；
- 必要时的参考图、关键图或少量拆片；
- 图片、动画或简单视频候选；
- Director 根据结果修正语义表达。

## 第四阶段：设计学习流程

完成：

- 场景播放；
- 音频呈现；
- 识别练习；
- 新场景迁移；
- 输出练习；
- 复习机制。

## 第五阶段：扩展资源与可选效果评估

比较：

- 翻译学习；
- 普通图片；
- 通用场景；
- 个性化场景。

这些实验用于优化资源，不作为产品方法或规模化的强制 gate。

## 第六阶段：自动化与规模化

在验证有效后，再开发：

- 语义场景编译器；
- 个性化生成系统；
- 大规模词义库；
- 学习者模型；
- 自适应课程路径。

------

# 最后，将所有工作压缩成一个系统图

```text
高频词与义项选择
        ↓
词义语义骨架建模
        ↓
相邻概念与 L1 差异分析
        ↓
通用场景组设计
        ↓
学习者经验与背景建模
        ↓
Director 将语义场景翻译为模型提示词
        ↓
视频生成与按需修正
        ↓
声音—场景学习单元
        ↓
对比、迁移与输出训练
        ↓
学习效果评估
        ↓
根据数据优化场景
        ↓
AI 自动生成与规模化
```

整个项目最重要的三层可以概括为：

> **第一层：通用语义骨架。**
> **第二层：个性化经验渲染。**
> **第三层：跨场景概念泛化。**

而最先要真正动手完成的，不是先堆出 30—50 个义项，也不是建设一套复杂制片系统，而是
稳定语义资源契约和 Director 能力：先用少数不同语义类型完成“语义规格—场景证据—
高质量模型提示词—视频生成与修正—可评分任务—资源导出”的闭环，再扩展规模和模型。
